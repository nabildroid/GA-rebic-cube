class Cube {
	constructor() {
		this.white = createGroup(200, 10, 100, COLORS.WHITE);
		this.yellow = createGroup(200, 120, 100, COLORS.YELLOW);
		this.green = createGroup(200, 230, 100, COLORS.GREEN);
		this.orange = createGroup(90, 230, 100, COLORS.ORANGE);
		this.blue = createGroup(310, 230, 100, COLORS.BLUE);
		this.red = createGroup(200, 340, 100, COLORS.RED);
	}
}
